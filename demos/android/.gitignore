# Gradle build outputs
.gradle/
app/build/
build/
local.properties

# Built native libraries (regenerated by cargo ndk)
app/src/main/jniLibs/

# Gradle wrapper jar (download with `gradle wrapper`)
gradle/wrapper/gradle-wrapper.jar
